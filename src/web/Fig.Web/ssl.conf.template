server {
    listen               $FIG_WEB_SSL_PORT ssl;
    ssl_certificate      $SSL_CERT_PATH;
    ssl_certificate_key  $SSL_KEY_PATH;
    ssl_protocols        TLSv1.3;
    include /etc/nginx/conf.d/fig-web-server-common.conf;
}
