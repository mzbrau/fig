@using Fig.Web.Models
<RadzenSwitch @bind-Value=@Setting.Value Change=@(args => Setting.ValueChanged(args.ToString())) />

@code
{
    [Parameter] public BoolSettingConfigurationModel Setting { get; set; }
}