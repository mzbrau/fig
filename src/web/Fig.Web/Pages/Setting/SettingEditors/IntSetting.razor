@using Fig.Web.Models.Setting.ConfigurationModels
<RadzenNumeric TValue="int" @bind-Value="@Setting.Value" Disabled="@Setting.IsReadOnly"
               @oninput="@(args => Setting.ValueChanged(args.ToString()))" Class="w-100"/>
<p class="@(Setting.IsValid ? "collapse" : "")" style="color: pink; font-size: small">@Setting.ValidationExplanation</p>

@code
{
    
    [Parameter]
    public IntSettingConfigurationModel Setting { get; set; }
}